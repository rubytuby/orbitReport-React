const Banner = () => {
    return (
        <div>
            <header>Orbit Report</header>
            <p>Use the buttons to see the satellites in the orbit type.</p>
        </div>
    );
};

export default Banner;











